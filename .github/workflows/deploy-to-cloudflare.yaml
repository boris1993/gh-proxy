name: Deploy to CloudFlare

on:
  workflow_dispatch:
  push:
    branches: ["upstream"]

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - uses: actions/checkout@v4
      - run: wget https://raw.githubusercontent.com/boris1993/gh-proxy/refs/heads/upstream/index.js
      - run: ls
      # - name: Publish
      #   uses: cloudflare/wrangler-action@v3
      #   with:
      #     apiToken: ${{ secrets.CF_API_TOKEN }}
